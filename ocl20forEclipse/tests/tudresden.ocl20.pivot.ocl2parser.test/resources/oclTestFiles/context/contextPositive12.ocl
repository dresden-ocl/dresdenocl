package package1::package2

-- The return Type of aSet is Type1,
-- but multiple, unique, and unordered.
-- Thus the Parser should interpret the Type as Set(Type1).
context Type1::aSet: Set(Type1)
derive: Type1.allInstances()
  
endpackage
