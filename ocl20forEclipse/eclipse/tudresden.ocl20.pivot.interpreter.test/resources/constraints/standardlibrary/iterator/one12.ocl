package package1

context Class1
inv:
/* Expected as false. */
Bag { 0, 1, 2, 0 } -> one(element: Integer | 
  if element = 1
    then booleanNull
    else element < 1
  endif
)

endpackage