package package1

context Class1
inv:
/* Expected as invalid. */
Bag { 0, 1, 2, 3 } -> isUnique(element: Integer | 
  if element = 1
    then integerNull
    else element
  endif
)

endpackage