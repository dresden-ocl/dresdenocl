package package1

context Class1
inv testOrderedSetEquals01: let os : OrderedSet(Integer) = OrderedSet{1,2,3,4,5} in 
	let so : OrderedSet(Integer) = OrderedSet{5,4,3,2,1}
		in os->sortedBy(e|-e) = OrderedSet{5,4,3,2,1}
 and os->sortedBy(e|-e) = os
 and os->sortedBy(e|e) = os->sortedBy(e|-e)
 and OrderedSet{1,2,3,4,5} = OrderedSet{5,4,3,2,1}
 and os = so

endpackage