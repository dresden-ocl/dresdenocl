package org::dresdenocl::examples::royalandloyal

-- Should not be parsed. @pre is only valid in postconditions.
context LoyaltyProgram::enroll(c : Customer)
pre: 
  participants = participants@pre->including(c)

endpackage