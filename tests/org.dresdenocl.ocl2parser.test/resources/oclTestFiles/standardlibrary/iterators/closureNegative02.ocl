package package1::package2

/* The body of closure has to result in the same type as the source's element type. */
context Type1
def: testClosureNegative02 : Set(Type1) =
self.asSet()->closure(true)

endpackage